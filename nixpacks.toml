[phases.setup]
nixPkgs = ["python311", "nodejs_20"]

[phases.install]
cmds = ["pip3 install -r requirements.txt", "npm install"]

[phases.build]
cmds = ["npx tailwindcss -i ./static/input.css -o ./static/output.css --minify"]
